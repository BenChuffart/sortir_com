{% extends "base.html.twig" %}

{% block body%}

    <h2>Create your trip :</h2>
    <div class="create">
        {{ form_start(tripForm) }}

        {{ form_widget(tripForm) }}

        <button type="submit"> Create !</button>

        {{ form_end(tripForm) }}
    </div>

{% endblock %}

{% block title%}
    Create a new trip !
{% endblock %}
